# Manuscript tables

This directory is for machine-readable exports of manuscript-facing tables. The current frozen source data are in `outputs/simulation/` and `outputs/criteo/`; `scripts/reproduce_manuscript_assets.py` writes formatted table CSVs here.
