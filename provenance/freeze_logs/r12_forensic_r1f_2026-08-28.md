# Decision Log — R1.2 Forensic Audit → R1F

R1.2 strict gate remains FAIL.

Direct audit of the uploaded R1.2 ZIP found:
- 54 policy rows, 9 winner rows, 60 calibration-bin rows, 30 PC1-decile rows;
- winner table internally matches the R1.2 gain table;
- all propensity overlap/calibration/weighted-SMD diagnostics pass;
- PC1-decile gate fails only in:
  * ESS=.8 decile 3: .010121;
  * ESS=.5 decile 4: .010553.

This is too small and isolated to justify another propensity learner, but a
predeclared failure cannot be relabeled PASS.

Role decision:
Criteo will no longer be presented as primary causal validation. It may proceed
as a secondary large-scale application/benchmark with the R1.2 limitation
explicitly reported.

Separate implementation finding:
R1.2 source-select policy rates match the original invalid R1 rates almost
exactly. The R1.1 tie correction was lost through decimal cutoff serialization
combined with exact equality.

R1F performs one technical materialization correction with frozen models and
frozen R1.2 propensity models. This does not alter the R1.2 scientific gate.
