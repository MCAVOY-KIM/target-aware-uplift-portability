# Data directory

Raw data must **not** be committed to GitHub.

Expected local layout:

```text
data/
└── raw/
    └── criteo-research-uplift-v2.1.csv.gz
```

The raw Criteo file is intentionally excluded from the repository. See `../docs/data_access.md`.
